[Unit]
Description=Check that Jottacloud sync is still moving

[Service]
Type=oneshot
ExecStart=%h/.local/bin/jotta-canary
# The canary waits UPLOAD_GRACE_SECONDS for the upload, so allow well over it.
TimeoutStartSec=20min
